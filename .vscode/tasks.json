{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "EMSCRIPTEN_BUILD",
            "type": "shell",
            "command": "emsdk activate; emcc src/main.cpp -o main.js -s NO_EXIT_RUNTIME=1 -s EXPORTED_RUNTIME_METHODS=[ccall] -s USE_SDL=2 -s MAX_WEBGL_VERSION=2",
        }
    ]
}
